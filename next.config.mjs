/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: {
    buildActivity: false,
    // buildActivityPosition can be set if you ever re-enable it
    // buildActivityPosition: 'bottom-right',
  },
};

export default nextConfig;
